package com.kagubuzz.weather.tests;

public class WeatherReportTest
{

	
	public static void main(String args[])
	{/*
		TBLKaguLocation kaguLocation = new Cartography().getKaguLocationFromUSZipCode("94536");
		WeatherReport weatherReport = new WeatherReport(kaguLocation, -480);
		Weather weather = weatherReport.getCurrentReport(null);
		System.out.println("Current temp =" + weather.currentTemp);
		List<Weather> weatherForecast = weatherReport.forecast(3);
		SimpleDateFormat sdf = new SimpleDateFormat("EEE, d MMM yyyy, hh:mm aaa");
		
		  
		for(Weather w: weatherForecast)
		{
			System.out.println("Weather for + " + sdf.format(w.reportDate.getTime()) + "." + "Hightemp:" + w.highTemp + "Lowtemp" + w.lowTemp + " descipt" + w.weatherDescription);
		}*/
	}

}
